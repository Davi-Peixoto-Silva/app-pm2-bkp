<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Erro no Sistema</title>
  <link rel="icon" href="https://santelisa.com.br/wp-content/themes/2024_santelisa/assets/ico/favicon-32x32.png">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css" rel="stylesheet"> <!-- Ícones Bootstrap -->

  <style>
    body {
      display: flex;
      align-items: center;
      justify-content: center;
      min-height: 100vh;
      background-color: #f0f2f5;
    }
    .error-container {
      max-width: 550px;
      text-align: center;
      padding: 40px;
      border-radius: 12px;
      background-color: #ffffff;
      box-shadow: 0 6px 20px rgba(0, 0, 0, 0.1);
    }
    .error-code {
      font-size: 6rem;
      font-weight: bold;
      color: #dc3545;
    }
    .error-message {
      font-size: 1.3rem;
      color: #495057;
      margin-bottom: 20px;
    }
    .error-details {
      font-size: 0.9rem;
      color: #6c757d;
      background-color: #fff3cd;
      padding: 10px;
      border-radius: 6px;
      margin-bottom: 20px;
    }
    .btn-home {
      font-weight: 500;
      padding: 8px 20px;
    }
    .contact-admin {
      margin-top: 20px;
      font-size: 0.95rem;
      color: #6c757d;
    }
  </style>
</head>
<body>
  <div class="error-container">
    <div class="error-code"><%= codigo || 500 %></div> <!-- Código de erro dinâmico -->
    <div class="error-message">Ocorreu um erro inesperado no sistema.</div>

    <% if (mensagem) { %>
      <div class="error-details">
        <%= mensagem %>
      </div>
    <% } %>

    <a href="/" class="btn btn-primary btn-home">
      <i class="bi bi-arrow-left"></i> Voltar para a Home
    </a>

    <div class="contact-admin">
      Se o problema persistir, por favor contate o Administrador do Sistema.
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
