<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <title>Download de Arquivos</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" rel="stylesheet">
  <link rel="icon" href="https://santelisa.com.br/wp-content/themes/2024_santelisa/assets/ico/favicon-32x32.png">
  <style>
    body {
      background-color: #f8f9fa;
      display: flex;
      justify-content: center;
      align-items: center;
      min-height: 100vh;
      font-family: 'Segoe UI', sans-serif;
    }

    .card {
      padding: 30px;
      border-radius: 15px;
      box-shadow: 0 0 20px rgba(0,0,0,0.1);
      max-width: 600px;
      width: 100%;
      background-color: #ffffff;
    }

    .btn i {
      margin-right: 8px;
    }

    .alert {
      font-size: 15px;
    }
  </style>
</head>
<body>

  <div class="card text-center">
    <h4 class="text-success mb-4">
      <i class="fas fa-file-invoice"></i> Arquivos para o Cliente
    </h4>
    <p><strong>Cliente:</strong> <%= cliente.Cliente %></p>
    <p><strong>CNPJ:</strong> <%= cnpj %></p>
    <p><strong>Nota Fiscal:</strong> <%= numero %></p>

    <div class="d-grid gap-3 mt-4">
      <% if (tem_nota === 'Sim') { %>
        <a href="<%= link_nota %>" class="btn btn-primary" target="_blank">
          <i class="fas fa-file-pdf"></i> Baixar Nota Fiscal
        </a>
      <% } %>

      <% if (tem_boleto === 'Sim') { %>
        <a href="<%= link_boleto %>" class="btn btn-secondary" target="_blank">
          <i class="fas fa-file-invoice-dollar"></i> Baixar Boleto
        </a>
      <% } %>

      <% if (tem_nota !== 'Sim' && tem_boleto !== 'Sim') { %>
        <div class="alert alert-warning mt-3">
          <i class="fas fa-exclamation-triangle"></i> Nenhum arquivo disponível para esta nota.
        </div>
      <% } %>
    </div>

    <!-- <a href="#" onclick="if (window.opener) { window.close(); } else { history.back(); }" class="btn btn-outline-dark mt-4">
      <i class="fas fa-arrow-left"></i> Fechar / Voltar
    </a> -->
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
